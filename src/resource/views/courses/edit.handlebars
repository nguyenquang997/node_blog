<h2>Cập nhật khóa học</h2>
<div class="mt-4">
    <form method="POST" action="/courses/{{coursesJson._id}}?_method=PUT">

        <div class="mb-3">
            <label for="exampleInputPassword1" class="form-label">Mã khóa học</label>
            <input type="text" name="slugurl" value="{{coursesJson.slugurl}}" class="form-control" placeholder="">
        </div>

        <div class="mb-3">
            <label for="exampleInputPassword1" class="form-label">Tên khóa Học</label>
            <input type="text" name="name" value="{{coursesJson.name}}" class="form-control" placeholder="">
        </div>

        <div class="mb-3">
            <label for="exampleInputPassword1" class="form-label">Thông tin khóa học</label>
            <input type="text" name="info" value="{{coursesJson.info}}" class="form-control" placeholder="">
        </div>

        <div class="mb-3">
            <label for="exampleInputPassword1" class="form-label">Video ID</label>
            <input type="text" name="videoId" value="{{coursesJson.videoId}}" class="form-control" placeholder="">
        </div>

        <button type="submit" class="btn btn-primary">Chỉnh sửa</button>
    </form>
</div>